class MemeGrenade_aiPai extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_aiPai()
	{
		SetAmmoType("MemeGrenade_aiPaiAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_atolaODedoNestaPorra extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_atolaODedoNestaPorra()
	{
		SetAmmoType("MemeGrenade_atolaODedoNestaPorraAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_bomDiaaa extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_bomDiaaa()
	{
		SetAmmoType("MemeGrenade_bomDiaaaAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_bomDiaNaFazendinhaPagode extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_bomDiaNaFazendinhaPagode()
	{
		SetAmmoType("MemeGrenade_bomDiaNaFazendinhaPagodeAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_grito extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_grito()
	{
		SetAmmoType("MemeGrenade_gritoAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_iluminat extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_iluminat()
	{
		SetAmmoType("MemeGrenade_iluminatAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_lala extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_lala()
	{
		SetAmmoType("MemeGrenade_lalaAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_lalaVaiSeFude extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_lalaVaiSeFude()
	{
		SetAmmoType("MemeGrenade_lalaVaiSeFudeAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_marioPeido extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_marioPeido()
	{
		SetAmmoType("MemeGrenade_marioPeidoAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_plantaoGlobo extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_plantaoGlobo()
	{
		SetAmmoType("MemeGrenade_plantaoGloboAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_putsputs extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_putsputs()
	{
		SetAmmoType("MemeGrenade_putsputsAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_risadaTerror extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_risadaTerror()
	{
		SetAmmoType("MemeGrenade_risadaTerrorAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_skibidi extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_skibidi()
	{
		SetAmmoType("MemeGrenade_skibidiAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_soDoI extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_soDoI()
	{
		SetAmmoType("MemeGrenade_soDoIAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_temaTristeChaves extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_temaTristeChaves()
	{
		SetAmmoType("MemeGrenade_temaTristeChavesAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_tictokIndia extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_tictokIndia()
	{
		SetAmmoType("MemeGrenade_tictokIndiaAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_wo extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemesGrenade_wo()
	{
		SetAmmoType("MemeGrenade_woAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_zeDaManga extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_zeDaManga()
	{
		SetAmmoType("MemeGrenade_zeDaMangaAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_veSeEnrrugou extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_veSeEnrrugou()
	{
		SetAmmoType("MemeGrenade_veSeEnrrugouAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_salve extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_salve()
	{
		SetAmmoType("MemeGrenade_salveAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_Pressao extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_Pressao()
	{
		SetAmmoType("MemeGrenade_PressaoAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}

class MemeGrenade_sexy extends FlashGrenade
{
	override void OnExplosionEffects(Object source, Object directHit, int componentIndex, string surface, vector pos, vector surfNormal, float energyFactor, float explosionFactor, bool isWater, string ammoType) {}

	void MemeGrenade_sexy()
	{
		SetAmmoType("MemeGrenade_sexyAmmo");
		SetFuseDelay(2);
	}

	void ~FlashGrenade() {}
}
